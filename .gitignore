.DS_Store
**/.DS_Store

# Privado / local — no publicar en el repo público
promptbuilder_export/conversation_history.txt
.claude/

# Secretos / entorno (repo público) — nunca subir claves reales
.env
.env.*
!.env.example

# Tooling local de QA (Playwright) — no forma parte de la app standalone
node_modules/
package.json
package-lock.json
